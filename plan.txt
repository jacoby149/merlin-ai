Make the opencode bootstrapper!
No fastapi to start...

Lets try opencode + oh my opencode...
I just never did it before.
   Lets use it to
   1. Make the linktree at the AI layer.
   2. switch the AI UI to bun
   3. Make a config page for ENV vars at
    4. Object maker layer
    5. App maker layer
   4. Config ENV vars so stuff navigates nicely.
   5. Make license key work at AI Layer!

Integrate Opencode into the project.
  Make a container that does opencode serve.

Make the license keys work properly
  UI
  API
Make the GPT API Key work too.
  UI
  API
Store that the license key was verified as a cookie.
  UI
  API
Store GPT API Key as a cookie too.
  UI
  API
Then, the thing works.
No ENV var needed.

Add undo/redo
Make the chat UI better.
  Show history + be bigger, i.e. half screen.
  give some feedback on what is happening, some more streaming kind of stuff.

Add library installing when libraries are broken.
Making Changes ...
  1. All Libraries look good
     OR
     Pandas missing, installing Pandas
  2. Envs Changed? 
    Restart Containers.
  3. Error? 
     Concise ai explanation. 
     or No Errors! Looks good!
4. Reply


Make monolithic deployment.

Make the marketing an ENV variable to have the UI to go on or off.
  Make the marketing page make more sense.

Make a docs page with instructions.

Add getting started + deployment videos
  Developers (Windows) (Mac)

